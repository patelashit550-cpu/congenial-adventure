# Copy to .env.orderbook or .env.local — never commit real values.
# Ethereum mainnet order book custody seed (12 or 24 words).
ORDERBOOK_HD_MNEMONIC="word1 word2 ... word12"

# Optional JSON-RPC for future deposit watchers (mainnet only).
ORDERBOOK_ETH_RPC_URL="https://ethereum.publicnode.com"
